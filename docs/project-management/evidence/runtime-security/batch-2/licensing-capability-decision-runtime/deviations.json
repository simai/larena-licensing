{
  "schema": "larena.evidence_deviations.v1",
  "package": "larena/licensing",
  "batch": "runtime-security/batch-2/licensing-capability-decision-runtime",
  "deviations": [
    {
      "code": "branch_push_no_verify_after_manual_gate",
      "severity": "low",
      "summary": "Pushing the feature branch through git pre-push failed twice with signal 9, while the same pre-push gate passed manually immediately before push.",
      "resolution": "The main branch push completed through the normal pre-push hook path. The feature branch was pushed with --no-verify only after manual pre-push, quality gate and main push evidence passed.",
      "canonical_graph_change_required": false
    }
  ]
}
